{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core access for Captura's local windows.",
  "windows": ["main", "capture-hud"],
  "permissions": ["core:default", "updater:default", "process:allow-restart"]
}

